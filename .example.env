# =================================================
# Example Environment Configuration
# =================================================
#
# Этот файл служит шаблоном для создания вашего локального .env файла.
# Скопируйте этот файл в .env и замените значения на необходимые.
# ---
# This file serves as a template for your local .env file.
# Copy this file to .env and replace the values as needed.

# --- Application Environment ---
# Определяет режим работы приложения. Влияет на уровень логирования (например, Development логи более подробные).
# Допустимые значения: "development", "production".
# По умолчанию: "development"
#
# Defines the application's operating mode. Affects logging levels (e.g., Development logs are more verbose).
# Allowed values: "development", "production".
# Default: "development"
ENV=development

# --- Server Port ---
# Порт, на котором будет запущен gRPC сервер.
# По умолчанию: 50051
#
# The port on which the gRPC server will run.
# Default: 50051
PORT=50051

# --- HTTP Timeout ---
# Таймаут для операций. Хотя в названии есть HTTP, в коде он может использоваться для любых операций, требующих ограничения по времени.
# Значение должно быть в формате, который парсит time.ParseDuration (например, "30s", "1m", "500ms").
# По умолчанию: "30s"
#
# Timeout for operations. Although the name includes HTTP, it can be used for any time-limited operation in the code.
# The value should be in a format that time.ParseDuration can parse (e.g., "30s", "1m", "500ms").
# Default: "30s"
HTTP_TIMEOUT=30s
